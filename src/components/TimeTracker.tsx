import React from 'react';
import { Text, View } from 'react-native';

const TimeTracker = () => {
  return (
    <View>
      <Text>TimeTracker</Text>
    </View>
  );
};

export default TimeTracker;
